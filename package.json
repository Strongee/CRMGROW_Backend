{
  "name": "teamgrow",
  "version": "1.0.0",
  "description": "Teamgrow backend",
  "main": "app.js",
  "scripts": {
    "start": "node ./bin/www",
    "dev": "nodemon ./bin/www",
    "dev:test": "NODE_ENV=test npm run dev",
    "start:test": "NODE_ENV=test npm run start",
    "lint": "eslint config controllers models routes",
    "lint-fix": "bash fixTab.sh && eslint config controllers models routes --fix",
    "deploy": "pm2 deploy ecosystem.config.json staging --force",
    "test": "rm -f /var/www/watog_api.com/test_watogDB.sqlite && NODE_ENV=test nyc --reporter=lcov ava test/**/*.test.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/teamgrow/backend_admin.git"
  },
  "keywords": [
    "teamgrow",
    "backend"
  ],
  "author": "superwebtop",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/teamgrow/backend_admin/issues"
  },
  "homepage": "https://github.com/teamgrow/backend_admin#readme",
  
  "dependencies": {
    "express": "^4.16.4"
  }
}
